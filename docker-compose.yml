version: '3'
services:
  nginx:
    restart: always
    build:
      context: ./nginx
    ports:
      - '3000:80'
  client:
    build:
      context: ./client
  api:
    build:
      context: ./server
